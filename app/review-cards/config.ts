// ── Google Review Cards: pricing + checkout config ───────────────────────────
//
// TODO(you): create the product in your Stripe Dashboard
//   (Products → Add product → "Google Review Card Kit", one-time price),
//   generate a Payment Link for it, and paste the URL below.
//
// While this is empty, the order flow still works: orders are recorded and
// emailed to you, and the buyer sees "we'll send you a payment link by email"
// — so nothing is lost before the Stripe product exists.
export const STRIPE_REVIEW_CARD_LINK = 'https://buy.stripe.com/8x2fZi0zM1Ww6Iu59sfrW0a'

// Shown on the landing page + order form. Keep in sync with the Stripe price.
export const PRICE_LABEL = '$29'
